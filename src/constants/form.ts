import { FormConfig } from "../types/form";

export const initialFormConfig: FormConfig = {
  title: "",
  description: "",
  fields: [],
  validation: true,
  helpText: true,
  placeholders: true,
};
